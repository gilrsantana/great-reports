/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { GroupDto } from '../../models/group-dto';

export interface ApiGroupsGet$Json$Params {
  groupLeaderId?: string;
}

export function apiGroupsGet$Json(http: HttpClient, rootUrl: string, params?: ApiGroupsGet$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<GroupDto>>> {
  const rb = new RequestBuilder(rootUrl, apiGroupsGet$Json.PATH, 'get');
  if (params) {
    rb.query('groupLeaderId', params.groupLeaderId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<GroupDto>>;
    })
  );
}

apiGroupsGet$Json.PATH = '/api/Groups';
