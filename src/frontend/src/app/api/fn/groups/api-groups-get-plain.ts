/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { GroupDto } from '../../models/group-dto';

export interface ApiGroupsGet$Plain$Params {
  groupLeaderId?: string;
}

export function apiGroupsGet$Plain(http: HttpClient, rootUrl: string, params?: ApiGroupsGet$Plain$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<GroupDto>>> {
  const rb = new RequestBuilder(rootUrl, apiGroupsGet$Plain.PATH, 'get');
  if (params) {
    rb.query('groupLeaderId', params.groupLeaderId, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: 'text/plain', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<GroupDto>>;
    })
  );
}

apiGroupsGet$Plain.PATH = '/api/Groups';
