/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ProviderDetailsDto } from '../../models/provider-details-dto';

export interface ApiProviderCompaniesIdGet$Json$Params {
  id: string;
}

export function apiProviderCompaniesIdGet$Json(http: HttpClient, rootUrl: string, params: ApiProviderCompaniesIdGet$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<ProviderDetailsDto>> {
  const rb = new RequestBuilder(rootUrl, apiProviderCompaniesIdGet$Json.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ProviderDetailsDto>;
    })
  );
}

apiProviderCompaniesIdGet$Json.PATH = '/api/ProviderCompanies/{id}';
